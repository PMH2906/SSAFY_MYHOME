<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.myhome.model.mapper.AptMapper">

	<select id="dongSearch" parameterType="string" resultType="dongcode">
		select * from dongCode where dongCode = #{dongCode}
	</select>
	
	<select id="houseInfoSearch" parameterType="string" resultType="houseinfo"> 
		select * from houseinfo where dongCode = #{dongCode}
	</select>
</mapper>
